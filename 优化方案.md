# OutlookAPI 优化方案

## 一、核心问题：数据库连接池耗尽（紧急）

### 现象

```
sqlalchemy.exc.TimeoutError: QueuePool limit of size 5 overflow 10 reached, 
connection timed out, timeout 30.00
```

同时影响 `/api/` 和 `/admin/` 两个路由，所有数据库操作全部阻塞。

### 根因分析

1. **SQLAlchemy 连接池未配置**：默认 `pool_size=5, max_overflow=10`，总共最多 15 个连接，对于并发请求+后台线程完全不够。

2. **线程中数据库 Session 未关闭**：`tasks.py` 和 `admin.py` 的 `ThreadPoolExecutor` 在每个线程里执行 `with app.app_context()` 并做 DB 查询，但线程结束时没有调用 `db.session.remove()`，导致连接泄漏。

3. **定时任务与 API 请求争抢连接**：定时刷新 Token 任务用 5 个线程并发，每个线程占一个 DB 连接，API 请求同时也需要连接，很容易把池子占满。

4. **`process_email_messages()` 持有长事务**：API 请求中先做 IMAP/Graph 网络 IO（可能耗时数秒），期间一直持有 DB Session，连接不释放。

### 修复方案

| 项目 | 修改内容 |
|------|----------|
| **config.py** | 添加连接池配置：`SQLALCHEMY_POOL_SIZE = 10`、`SQLALCHEMY_MAX_OVERFLOW = 20`、`SQLALCHEMY_POOL_RECYCLE = 1800`、`SQLALCHEMY_POOL_PRE_PING = True` |
| **app.py** | 注册 `@app.teardown_appcontext` 确保每次请求结束后 `db.session.remove()` |
| **tasks.py** | 每个线程函数结尾添加 `db.session.remove()`，确保连接归还池中 |
| **admin.py** | `refresh_single_email_token()` 线程函数结尾添加 `db.session.remove()` |
| **api.py** | 将 DB 查询与网络 IO 分离——先查库获取所需字段，关闭 Session，再做 IMAP 操作 |

---

## 二、邮箱登录双重 Token 请求（性能浪费）

### 现象

每次 `OutlookGraphClient.login()` 会发起 **2 次** HTTP Token 请求。

### 根因

`OutlookGraphClient.login()` 先调用 `_get_access_token()` 获取 access_token，然后创建 `OauthEmailReceiver` 并设置 `self.ec.access_token = access_token`。  
但 `OauthEmailReceiver.login()` 内部又调用 `self.get_accesstoken(self.passwd)` **覆盖了** 刚设置的 access_token，导致重复请求。

### 修复方案

修改 `OauthEmailReceiver.login()` 增加逻辑：如果 `self.access_token` 已设置，直接用现有 token 登录 IMAP，跳过 HTTP 获取步骤。

```python
def login(self):
    if not self.access_token:
        self.access_token = self.get_accesstoken(self.passwd)
    if not self.access_token:
        return False
    try:
        self.mail = imaplib.IMAP4_SSL('outlook.live.com')
        self.mail.authenticate('XOAUTH2', lambda x: self.generate_auth_string(self.email, self.access_token))
        return True
    except:
        return False
```

**效果**：每次登录减少 1 次 HTTP 请求（微软 Token 端点响应约 0.5-2s），提升 API 响应速度。

---

## 三、IMAP 客户端缓存问题

### 现象

API 缓存的 IMAP 客户端 10 分钟后过期被清理，但 IMAP 连接未正确关闭；同时缓存字典非线程安全。

### 问题点

1. `client_cache` 是普通 `dict`，多线程并发读写存在竞态条件。
2. 缓存过期时只 `del` 了字典项，未调用 `mail.logout()` 关闭 IMAP 连接，造成服务端连接泄漏。
3. `get_last_email()` 中虽有 `noop()` 检测断连并重登录，但重登录又会产生新的 Token 请求。

### 修复方案

| 项目 | 修改内容 |
|------|----------|
| **线程安全** | 用 `threading.Lock` 保护 `client_cache` 的读写 |
| **连接清理** | 过期清理时调用 `client.ec.mail.logout()` 关闭 IMAP 连接 |
| **缓存上限** | 添加最大缓存数量限制（如 100），超出时清理最旧的缓存，防止内存无限增长 |

---

## 四、Async/Sync 混用导致复杂度高

### 现象

`refresh_email_token()` 是 `async` 函数，但所有调用方都在同步线程中用 `asyncio.new_event_loop()` + `loop.run_until_complete()` 来调用，每个线程都创建/销毁一个事件循环。

### 根因

`do_refresh_token()` 用了 `httpx.AsyncClient`，导致上游连环 async。但实际场景不需要异步——每个线程内就一个请求。

### 修复方案

将 `do_refresh_token()` 改为同步函数，用 `requests` 或 `httpx`（同步模式）：

```python
def do_refresh_token(client_id, refresh_token):
    data = {
        'client_id': client_id,
        'grant_type': 'refresh_token',
        'refresh_token': refresh_token
    }
    resp = requests.post(
        'https://login.microsoftonline.com/consumers/oauth2/v2.0/token',
        data=data, timeout=30
    )
    res_json = resp.json()
    if res_json.get('refresh_token'):
        return res_json
    return None
```

**连带修改**：`refresh_email_token()` 去掉 `async`，所有调用方删除 `asyncio` 相关代码，直接同步调用。

---

## 五、API 接口健壮性

### 5.1 缺少网络超时

`OauthEmailReceiver.get_accesstoken()` 和 `OutlookGraphClient._get_access_token()` 的 `requests.post()` 没有设置 `timeout`，如果微软接口卡住，线程/请求会无限等待。

**修复**：所有 `requests` 调用添加 `timeout=30`。

### 5.2 API 500 错误未妥善处理

`/api/` 的 `except` 虽然捕获了异常返回 500 JSON，但 DB Session 可能处于脏状态。

**修复**：在 except 中添加 `db.session.rollback()`。

### 5.3 admin 页面一次加载全部邮箱

`admin.index` 执行 `OutlookEmail.query.all()`，当邮箱量大时会一次性加载所有数据到内存。

**修复**：改为分页查询，使用 `.paginate()` 并配合前端分页展示。

---

## 六、修改优先级

| 优先级 | 修改项 | 影响 | 工作量 |
|--------|--------|------|--------|
| P0 | 连接池配置 + Session 清理 | 直接解决 500 错误 | 小 |
| P0 | 线程函数中 `db.session.remove()` | 直接解决连接泄漏 | 小 |
| P1 | 双重 Token 请求修复 | 登录速度提升 50% | 小 |
| P1 | requests 添加 timeout | 防止线程卡死 | 小 |
| P1 | client_cache 线程安全 + 连接清理 | 防止竞态和连接泄漏 | 中 |
| P2 | async 改 sync | 降低代码复杂度 | 中 |
| P2 | admin 分页 | 大数据量下性能 | 中 |

---

## 七、修改涉及的文件清单

| 文件 | 修改内容 |
|------|----------|
| `config.example.py` | 添加 SQLAlchemy 连接池相关配置项 |
| `app.py` | 添加 `teardown_appcontext` 清理 Session |
| `tasks.py` | 线程函数添加 `db.session.remove()`；去除 async 调用 |
| `blueprints/admin.py` | 线程函数添加 `db.session.remove()`；去除 async 调用 |
| `blueprints/api.py` | 缓存加锁、过期清理关闭连接、DB 查询与 IO 分离 |
| `utils.py` | `refresh_email_token` 改为同步函数 |
| `tools/refresh_outlook.py` | `do_refresh_token` 改为同步函数 |
| `tools/imap_email.py` | `OauthEmailReceiver.login()` 跳过重复 token 获取；所有 requests 加 timeout |
